'''
Takes the files that are generated in debug_output/ and creates some graphs that are easier to inspect.

'''

def create_graph_animation_for_domain_field_quantity(field_quantity : str, domain_name  : str, time_step : float):
    '''
    Reads the file in debug_output for a specific output entry; Creates a graph for the given domain's field quantity at a time step.
    '''

    filename = domain_name + "_" + domain_name + "_t"


if __name__=="__main__":
